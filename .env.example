# Base URL for an upstream scraping or proxy API.
# If left empty, HTTP calls should be made directly to fully qualified URLs.
SCRAPER_BASE_URL=

# Log level: debug, info, warn, error
LOG_LEVEL=info

# When true, you can implement optional mock behavior inside endpoints.
MOCK_MODE=false